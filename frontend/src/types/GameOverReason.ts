export enum GameOverReason {
    checkmate,
    resigning,
    timeoutWin,
    timeoutDraw,
    stalemate,
    deadPosition,
    agreeDrawAfterOffer,
    fiftyMoveRule,
    threeFoldRepetitionRule,
}
